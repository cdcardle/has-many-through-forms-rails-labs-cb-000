<h2><%= @post.title %></h2>

<h3><%= @post.content %></h3>

<h4>Comments:</h4>
<p>
  <% @post.comments.each do |comment| %>
    <%= comment.content %> - <%= comment.user.username%><br>
  <% end %>
</p>

<!-- <h4>New Comment:</h4>
<%= form_for @post do |f| %>
  <%= fields_for :comments, @post.comments.build do |comments_fields| %>
    <%= comments_fields.text_field :content %>
    <%= comments_fields.collection_select :user_id, User.all, :id, :username %>
  <% end %>
  <%= f.submit %>
<% end %> -->

<h4>Users who commented:</h4>
<p>
  <% users = [] %>
  <% @post.comments.each do |comment| %>
    <% users << comment.user if comment.user.username %>
  <% end %>
  <% users.uniq.each do |user| %>
    <%= link_to user.username, user_path(user) %><br>
  <% end %>
</p>
